@using Sindemed.Models.Repositories
@using App_Dominio.Repositories
@model System.Web.Mvc.PagedList<Sindemed.Models.Repositories.NaoLocalizadoCorreioViewModel>

@{
    Layout = null;
}
@if (Model.Count > 0)
{
    <div class="row">
        <div class="col-md-12 column">
            <table class="table table-hover table-bordered table-condensed" style="cursor: pointer">
                <caption></caption>
                <thead style="background-color: #fff1a5">
                    <tr>
                        <th></th>
                        <th>Motivo</th>
                        <th>Código</th>
                    </tr>
                </thead>
                @foreach (Sindemed.Models.Repositories.NaoLocalizadoCorreioViewModel cor in Model)
                {
                    <tr>
                        <td style="width: 15%">
                            <div class="row-clearfix">
                                <div class="col-md-12">
                                    <div class="btn-group btn-group-xs">
                                        <button class="btn btn-sm" type="button">
                                            <em class="glyphicon glyphicon-edit"></em> @Html.ActionLink("Editar", "Edit", routeValues: new { correioId = cor.correioId }, htmlAttributes: new { @style = "text-decoration: none; color: #646464" })
                                        </button>
                                        <button class="btn btn-sm" type="button">
                                            <em class="glyphicon glyphicon-remove"></em> @Html.ActionLink("Excluir", "Delete", routeValues: new { correioId = cor.correioId }, htmlAttributes: new { @style = "text-decoration: none; color: #646464" })
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </td>
                        <td>
                            @cor.descricao
                        </td>
                        <td>
                            @cor.codigo
                        </td>
                    </tr>
                }
            </table>
        </div>
    </div>
    <div class="row" style="height: 50px">
        <div class="col-md-12">
        </div>
    </div>
    @Html.Partial("Paginacao", Model)
}
