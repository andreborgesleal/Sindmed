@model System.Web.Mvc.PagedList<Sindemed.Models.Repositories.MedicoViewModel>
@{
    Layout = null;
}

@if (Model != null)
{
    <div class="row Scroll" style="margin-top: -15px">
        <div class="col-md-12" style="padding-right: 0px;">
            <table class="table table-hover table-bordered table-condensed" style="cursor: pointer">
                <caption></caption>
                <thead style="background-color: #fff1a5">
                    <tr>
                        <th>Nome</th>
                    </tr>
                </thead>
                @foreach (Sindemed.Models.Repositories.AssociadoViewModel ass in Model)
                {
                    <tr onclick="Move('@ass.associadoId', '@ass.nome')">
                        <td>
                            <div>
                                @ass.nome
                            </div>
                        </td>
                    </tr>
                }
            </table>
        </div>
    </div>
    @Html.Partial("Paginacao", Model)
}
else
{
    @Html.Partial("_AcessoNegadoModal")
}








